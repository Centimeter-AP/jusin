1. InitializePlayer()
	Player클래스에서 값을 넣는 방식이 나을듯
	new로 만들어둬도 값을 덮어쓰는건 상관 없으니까
	Initialize()에서 Player->selectjob?
	굳이 생성자로 값을 넣을 필욘 없을듯 너무 길어진다
	음? 뭐 크게 상관.. 없어보인다?

2. enum 열거체도 클래스 public에 넣어둬도 괜찮을 것 같음
	직업 열거체라던지 

3. player의 변수들은 구조체에...?
	이거 꼭 해야함?
	난하기싫다
	ㅎㅎ
	이러면 get함수 만들 때 get구조체만 만들어도 된다곤 하는데
	그러면 변수 넣는 의미가 ㅇ있나?

4. 세이브 로드 지금 배운거 C언어 기반이니까 신경쓰지말것
	으아앙

5. 필드 클래스 나누실 것 같았음 
	필드에 player 클래스의 주소를 전달?
	어차피주소전달아닌가?헤헤

6. 생성자 호출 하면서 0 초기화는 그냥 memset으로?
	ㄱㅊ나?ㅎㅎ

7. 소멸자에 Release() 호출하자 네에

8. 오늘 배운 전방 선언으로 Attack 수정