#include "pch.h"
#include "CWallMgr.h"
#include "Define.h"
#include "CAbstractFactory.h"
#include "CWall.h"

void CWallMgr::Picking_Wall(POINT pt, int iDrawID, int iOption)
{
	//int		x = pt.x / TILECX;
	//int		y = pt.y / TILECY;

	//int	iIndex = y * TILEX + x;

	//if (0 > iIndex || (size_t)iIndex >= m_vecWall.size())
	//	return;

	//CObj* pWall = CAbstractFactory<CWall>::Create(x, y);
	//m_vecWall.push_back(pWall);
}
